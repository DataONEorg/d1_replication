<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
   	xmlns:context="http://www.springframework.org/schema/context"
  	xsi:schemaLocation="http://www.springframework.org/schema/beans 
  		http://www.springframework.org/schema/beans/spring-beans.xsd
    	http://www.springframework.org/schema/context  
    	http://www.springframework.org/schema/context/spring-context-3.0.xsd">
    	

    <bean id="replicaAuditDao" class="org.dataone.service.cn.replication.audit.ReplicaAuditDaoMetacatImpl">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <bean id="dataSource" destroy-method="close" class="org.apache.commons.dbcp.BasicDataSource">
        <property name="driverClassName" value="${replication.datasource.driverClassName}"/>
        <property name="url" value="${replication.datasource.url}"/>
        <property name="username" value="${replication.datasource.username}"/>
        <property name="password" value="${replication.datasource.password}"/>
        <property name="initialSize" value="${replication.datasource.initialSize}"/>
		<property name="maxActive" value="${replication.datasource.maxSize}"/>
    </bean>

</beans>